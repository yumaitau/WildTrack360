# @tanstack/charts lists multi-framework peers as optional (react^19, vue, etc.).
# With react@18 installed, npm still ERESOLVEs the optional peer range conflict.
# Allow install against the project's React 18 until a React 19 upgrade.
legacy-peer-deps=true
